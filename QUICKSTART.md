# Быстрый старт - Умная система распределения задач

## 📋 Что нужно сделать

### 1. Настроить Google Таблицу

Ваша Google Таблица теперь будет иметь **2 листа**:

#### Лист 1: "Специалисты"
```
| ID | Имя              | Telegram ID | Статус   | Специализация    |
|----|------------------|-------------|----------|------------------|
| 1  | Иван Иванов      | 123456789   | Активен  | Дезинфекция      |
| 2  | Петр Петров      | 987654321   | Активен  | Дератизация      |
| 3  | Сидор Сидоров    | 555555555   | Активен  | Дезинфекция      |
```

#### Лист 2: "Расписание" (создается автоматически)
Система автоматически заполняет этот лист при назначении задач.

### 2. Запустить систему

```bash
cd amocrm-integration
python src/main.py
```

### 3. Получить токены AmoCRM

1. Откройте в браузере: `http://localhost:5000/amocrm/auth`
2. Авторизуйтесь в AmoCRM
3. Скопируйте токены в `.env` файл

### 4. Настроить Telegram бота

Специалисты должны:
1. Найти вашего бота в Telegram
2. Отправить команду `/start`
3. Получить подтверждение регистрации

## 🎯 Как работает умное распределение

### Автоматический режим

Когда создается новая сделка в AmoCRM:

1. ✅ Система получает webhook
2. ✅ Анализирует всех специалистов
3. ✅ Выбирает лучшего по критериям:
   - Специализация
   - Свободное время
   - Загруженность
   - Близость к другим задачам
4. ✅ Создает задачу в Notion
5. ✅ Добавляет в расписание Google Sheets
6. ✅ Отправляет уведомление в Telegram

### Ручной режим

Через API можно создать тестовую задачу:

```bash
curl -X POST http://localhost:5000/api/test/create-task \
  -H "Content-Type: application/json" \
  -d '{
    "address": "Москва, ул. Ленина, 1",
    "phone": "+7 999 999-99-99",
    "date": "2026-05-10",
    "time": "10:00",
    "work_type": "Дезинфекция",
    "pests": "Тараканы",
    "work_time": 60,
    "cost": 5000
  }'
```

## 📱 Команды для специалистов в Telegram

| Команда | Описание |
|---------|----------|
| `/start` | Регистрация в системе |
| `/schedule` | Расписание на сегодня |
| `/schedule_tomorrow` | Расписание на завтра |
| `/schedule_week` | Расписание на неделю |
| `/tasks` | Активные задачи |
| `/help` | Справка |

## 🔍 Пример работы

### Сценарий 1: Новая сделка в AmoCRM

**Входные данные:**
- Адрес: Москва, ул. Ленина, 10
- Дата: 2026-05-10
- Время: 14:00
- Вид работ: Дезинфекция
- Время: 60 минут

**Что происходит:**

1. Система анализирует специалистов:
   - Иван (Дезинфекция) - свободен в 14:00 ✅
   - Петр (Дератизация) - занят в 14:00 ❌
   - Сидор (Дезинфекция) - свободен, но уже 6 часов работы ⚠️

2. Выбирает Ивана (лучшая оценка)

3. Иван получает в Telegram:
```
🆕 Новая задача!

📍 Адрес: Москва, ул. Ленина, 10
📅 Дата: 2026-05-10
🕐 Время: 14:00
📞 Телефон: +7 999 999-99-99
💼 Вид работ: Дезинфекция
🐛 Вредители: Тараканы
⏱ Время на работу: 60 мин
💰 Стоимость: 5000 ₽

[✅ Выполнено] [⚠️ Проблема]
```

### Сценарий 2: Специалист смотрит расписание

Иван отправляет `/schedule`:

```
📅 Ваше расписание на сегодня:

1. 10:00 - Москва, ул. Ленина, 1
   💼 Дезинфекция
   🐛 Тараканы
   📞 +7 999 999-99-99
   ⏱ 60 мин | 💰 5000 ₽

2. 14:00 - Москва, ул. Ленина, 10
   💼 Дезинфекция
   🐛 Тараканы
   📞 +7 888 888-88-88
   ⏱ 60 мин | 💰 5000 ₽

━━━━━━━━━━━━━━━━━━━━
📊 Итого: 2 задач(и)
⏱ Общее время: 120 мин (2ч 0м)
💰 Общая сумма: 10000 ₽
```

## 🎨 Настройка алгоритма

Если нужно изменить приоритеты, отредактируйте `src/smart_scheduler.py`:

```python
def calculate_specialist_score(self, specialist: Dict, task_data: Dict) -> float:
    score = 100.0
    
    # Измените веса под ваши нужды:
    
    # Специализация (по умолчанию: 30)
    if work_type in specialization:
        score += 30  # ← Увеличьте, если специализация важнее
    
    # Доступность времени (по умолчанию: 40)
    if not available:
        score -= 40  # ← Увеличьте, чтобы строже проверять время
    
    # Загруженность (по умолчанию: 20)
    if workload < 240:
        score += 10  # ← Измените пороги загруженности
    
    # География (по умолчанию: 10)
    if distance < 10:
        score += 10  # ← Увеличьте, если география важна
```

## 📊 Мониторинг системы

### Проверить работу системы:

1. **Health check:**
```bash
curl http://localhost:5000/health
```

2. **Список специалистов:**
```bash
curl http://localhost:5000/api/specialists
```

3. **Логи:**
```bash
python src/main.py
# Смотрите вывод в консоли
```

## 🚨 Частые вопросы

**Q: Что если все специалисты заняты?**
A: Система выберет специалиста с наименьшей загруженностью, даже если он занят.

**Q: Можно ли вручную переназначить задачу?**
A: Да, через API:
```bash
curl -X POST http://localhost:5000/api/tasks/assign \
  -H "Content-Type: application/json" \
  -d '{"page_id": "notion_page_id", "specialist_id": 2}'
```

**Q: Как добавить нового специалиста?**
A: Просто добавьте строку в Google Таблицу на листе "Специалисты".

**Q: Учитывается ли время в пути?**
A: Пока используется упрощенный расчет по совпадению адресов. Для точного расчета можно добавить Google Maps API.

## 🎯 Следующие шаги

1. Протестируйте систему с тестовыми задачами
2. Добавьте реальных специалистов в таблицу
3. Настройте webhook в AmoCRM
4. Запустите на сервере (Render.com или Railway.app)
5. Мониторьте работу первые дни

## 💡 Советы

- Начните с небольшого количества специалистов
- Проверьте, что все Telegram ID корректные
- Убедитесь, что специалисты запустили бота (`/start`)
- Следите за логами первые дни
- Корректируйте веса алгоритма под ваши задачи
